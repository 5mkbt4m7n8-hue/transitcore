(function(root){"use strict";const encoder=new TextEncoder(),table=(()=>{const values=new Uint32Array(256);for(let n=0;n<256;n++){let c=n;for(let k=0;k<8;k++)c=c&1?0xedb88320^(c>>>1):c>>>1;values[n]=c>>>0}return values})(),crc32=bytes=>{let crc=0xffffffff;for(const byte of bytes)crc=table[(crc^byte)&255]^(crc>>>8);return(crc^0xffffffff)>>>0},u16=(view,offset,value)=>view.setUint16(offset,value,true),u32=(view,offset,value)=>view.setUint32(offset,value>>>0,true);function createZip(files){const chunks=[],central=[];let offset=0;for(const file of files){const name=encoder.encode(file.name),data=encoder.encode(file.content),crc=crc32(data),local=new Uint8Array(30+name.length),view=new DataView(local.buffer);u32(view,0,0x04034b50);u16(view,4,20);u16(view,6,0x0800);u32(view,14,crc);u32(view,18,data.length);u32(view,22,data.length);u16(view,26,name.length);local.set(name,30);chunks.push(local,data);central.push({name,data,crc,offset});offset+=local.length+data.length}const centralStart=offset;for(const file of central){const header=new Uint8Array(46+file.name.length),view=new DataView(header.buffer);u32(view,0,0x02014b50);u16(view,4,20);u16(view,6,20);u16(view,8,0x0800);u32(view,16,file.crc);u32(view,20,file.data.length);u32(view,24,file.data.length);u16(view,28,file.name.length);u32(view,42,file.offset);header.set(file.name,46);chunks.push(header);offset+=header.length}const end=new Uint8Array(22),view=new DataView(end.buffer);u32(view,0,0x06054b50);u16(view,8,central.length);u16(view,10,central.length);u32(view,12,offset-centralStart);u32(view,16,centralStart);chunks.push(end);return new Blob(chunks,{type:"application/zip"})}root.TransitCoreZip={createZip}})(globalThis);
