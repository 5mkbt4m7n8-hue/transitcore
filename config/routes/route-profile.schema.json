{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://transitcore.local/schemas/route-profile-v1.json",
  "title": "TransitCore Route Profile v1",
  "type": "object",
  "required": ["schemaVersion", "id", "name", "provider", "line", "display"],
  "properties": {
    "schemaVersion": { "const": 1 },
    "id": { "type": "string" },
    "name": { "type": "string" },
    "provider": {
      "type": "object",
      "required": ["codespaceId", "vehicleEndpoint"],
      "properties": {
        "codespaceId": { "type": "string" },
        "vehicleEndpoint": { "type": "string", "format": "uri" }
      }
    },
    "line": {
      "type": "object",
      "required": ["publicCode", "mode"],
      "properties": {
        "id": { "type": "string" },
        "publicCode": { "type": "string" },
        "mode": { "enum": ["tram", "bus", "rail", "metro", "ferry", "other"] },
        "longName": { "type": "string" }
      }
    },
    "directions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "label", "destinationMatches", "color"],
        "properties": {
          "id": { "type": "string" },
          "label": { "type": "string" },
          "destinationMatches": { "type": "array", "items": { "type": "string" } },
          "color": { "type": "string" },
          "reverseShape": { "type": "boolean" }
        }
      }
    },
    "stops": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "lat", "lon"],
        "properties": {
          "id": { "type": "string" },
          "name": { "type": "string" },
          "lat": { "type": "number" },
          "lon": { "type": "number" },
          "gpio": { "type": "integer", "minimum": 0 },
          "vled": { "type": "integer", "minimum": 0 },
          "shapeDistanceMeters": { "type": "number", "minimum": 0 },
          "segmentToNext": {
            "type": "object",
            "required": ["vledStart", "vledCount"],
            "properties": {
              "vledStart": { "type": "integer", "minimum": 0 },
              "vledCount": { "type": "integer", "minimum": 1 }
            }
          }
        }
      }
    },
    "shape": {
      "type": "array",
      "minItems": 2,
      "items": {
        "type": "object",
        "required": ["lat", "lon"],
        "properties": {
          "lat": { "type": "number" },
          "lon": { "type": "number" }
        }
      }
    },
    "display": {
      "type": "object",
      "required": ["layout"],
      "properties": {
        "layout": { "enum": ["route-leds", "route-live", "vehicle-cards"] },
        "showVled": { "type": "boolean" }
      }
    }
  }
}


